# X-Change AI業務管理システム

**吉田自動車工業 X-Changeタイヤ販売店舗向け プロトタイプ**

> 補助金申請前 説明資料 兼 プロトタイプ仕様書 (v3.18.0 第1分割 / 店舗側スマホ画面整理・仕入登録・本日今月の状況・マスタ管理 追加版)
> **店舗側はスマートフォン操作 / 本社側はPC操作を想定。スマホでも本社側の確認が可能。**
> **売上音声入力はスマホ標準キーボードのマイクを利用するため、音声認識API料金は不要です。**
> **経費レシートOCRは Tesseract.js によるブラウザ内処理のためAPIキーは不要、画像は外部送信されません（v3.17）。**

---

## 1. システム概要

| 項目 | 内容 |
|---|---|
| **システム名** | X-Change AI業務管理システム |
| **対象店舗** | 吉田自動車工業 X-Changeタイヤ販売店舗 |
| **対象業務** | タイヤ販売売上管理 / 経費レシート管理 / 本社確認業務 / 修正依頼業務 / 月次集計 / CSV出力 |
| **利用者** | 店舗店長 / 本社事務 / 管理者 |
| **導入目的** | 店長1名で運営される店舗の売上・経費登録をAIで効率化し、本社へ即時共有することで、月次決算と利益把握を加速する |
| **現在のプロトタイプ範囲** | 画面構成 / 業務フロー / 即時反映 / モックAI / **実OCR（ブラウザ内 Tesseract.js）** / CSV出力（実APIは未接続） |
| **本番化時に追加する機能** | 実音声認識 / 高精度OCR（Google Cloud Vision / Document AI） / Googleサービス連携 / 認証・権限・バックアップ・セキュリティ対策（詳細は §8） |

---

## 2. 現在の業務課題

吉田自動車工業様の X-Change タイヤ販売店舗の運営において、従来の業務には以下のような課題があります。

- **拠点分散**: タイヤ販売店舗は本社とは別拠点で運営されている
- **少人数運営**: 店長1名で店舗運営を行っている
- **手作業中心**: 売上報告が紙伝票・Excel での手作業になりやすい
- **レシート管理の遅延**: 経費レシートの提出や確認が遅れやすい
- **本社確認の遅延**: 本社側で売上・経費の確認に時間がかかる
- **月次決算の遅れ**: 月次決算や利益把握が翌月以降にずれ込む
- **利益の見えにくさ**: タイヤ販売部門単体の利益がリアルタイムに見えにくい

これらの課題を、AI と OCR を活用した業務管理システムで解決します。

---

## 3. 導入後の業務フロー

### 3-1. 売上フロー

```
[店舗] 1. 店長が売上内容を 音声 または テキスト で入力
        ↓
       2. AI が売上項目（商品名 / サイズ / 数量 / 単価 / 合計 / 支払方法 / 車種等）に自動分解
        ↓
       3. 店長が画面で内容確認・必要に応じて修正
        ↓
       4. 「この内容で登録」ボタンで売上登録
        ↓
[本社] 5. 売上一覧に 即時反映（未確認）
        ↓
       6. 本社事務が内容確認 → 「確認済み」 へ
        ↓
       7. 内容に問題があれば 「修正依頼」 をコメント付きで送信
        ↓
[店舗] 8. 修正依頼を受信 → 内容を修正して 「再提出」 → 再び未確認へ
```

### 3-2. 経費フロー

```
[店舗] 1. 店長が レシート画像 をアップロード（撮影 or サンプル選択）
        ↓
       2. OCR がレシートから 日付 / 購入先 / 金額 / 消費税 / 支払方法 / 内容 を読取
        ↓
       3. AI が 12 カテゴリから 経費科目を候補分類（Top3 + 全候補）
        ↓
       4. 店長が内容確認・必要に応じて修正
        ↓
       5. 「この内容で登録」ボタンで経費登録
        ↓
[本社] 6. 経費一覧に 即時反映（未確認、AI分類科目のみ確定済み）
        ↓
       7. 本社事務が 「本社確定科目」 を最終決定（必要なら変更）
        ↓
       8. 「確認済み」 へ
        ↓
       9. 内容に問題があれば 「修正依頼」 をコメント付きで送信
```

### 3-3. 月次フロー

```
[本社] 1. 月次集計画面で売上・経費を確認
        ↓
       2. 未確認・修正依頼中のデータを処理（確認済み / 差し戻し）
        ↓
       3. 月次集計（売上合計・経費合計・粗利益・件数・内訳）を確認
        ↓
       4. 売上CSV / 経費CSV / 月次集計CSV を出力
            ↓ （会計ソフト連携・補助金申請資料に利用）
       5. 「月次締め」 ボタンでその月のデータを 「月次処理済み」 へロック
```

---

## 4. 主な機能一覧

### 4-1. 店舗側機能（店長がスマートフォンで操作）

| カテゴリ | 機能 |
|---|---|
| ダッシュボード | 店舗ダッシュボード（本日売上・本日経費・本日粗利益・未確認件数・修正依頼件数） |
| 売上 | 音声売上入力 / AI売上項目分解 / 売上登録確認画面 / 本日売上一覧 |
| 経費 | レシート画像アップロード / OCR読取結果表示 / AI経費分類候補 / 経費登録確認画面 / 本日経費一覧 |
| 修正対応 | 本社からの修正依頼確認 / 修正して再提出 |

### 4-2. 本社側機能（本社事務がPCで操作）

| カテゴリ | 機能 |
|---|---|
| ダッシュボード | 本社ダッシュボード（8KPI: 本日／今月の売上・経費、未確認件数、修正依頼中件数 等） |
| 一覧 | 売上一覧 / 経費一覧 / 未確認一覧（売上経費混在・タブ切替） |
| 操作 | 詳細確認 / 確認済み処理 / 修正依頼送信 / 本社確定科目変更 |
| 月次 | 月次集計 / 月次締め処理 |
| 出力 | 売上CSV出力 / 経費CSV出力 / 月次集計CSV出力 |

---

### 4-3. 開発フェーズ分割（第1〜第3分割で段階実装）

> 📌 **第1分割（v3.18.0）では、店舗側スマホ画面を店長が現場で使いやすい形に整理し、売上登録・経費登録・仕入登録・本日の状況・月初から今日現在の状況・最低限のマスタ管理を中心に構成します。店舗側では入力項目を増やしすぎず、詳細項目はアプリ内部で自動補完し、Googleスプレッドシート仕様に合わせたデータ構造で保存・送信します。顧客マスタは今後の顧客管理・再来店管理・車両情報管理に活用する前提で追加します。**

| 分割 | 範囲 | バージョン |
|---|---|---|
| **第1分割** ✅ | 店舗側スマホ画面整理・売上/経費/仕入の3登録・本日の状況・今月の状況・マスタ管理（顧客/商品/経費科目/支払方法）・Sheets送信状態の3レコード対応 | v3.18.0 |
| 第2分割（予定） | 本社確認ステータスの詳細操作・修正依頼フローの完全実装・決算用集計画面 | 未着手 |
| 第3分割（予定） | 操作ログ画面の完成・Google Sheets へのマスタ完全同期・権限管理（ログイン認証・店舗/本社/管理者ロール） | 未着手 |

#### 第1分割で追加された店舗側スマホ画面

| 画面 | 目的 | 主な内容 |
|---|---|---|
| 店舗ホーム メニュー (整理) | 店長が「登録・今日見る・今月見る・修正依頼を見る」だけで使える導線 | 売上を登録 / 経費を登録 / 仕入を登録 / 本日の状況 / 今月の状況 / 本日の売上一覧 / 本日の経費一覧 / 本社からの修正依頼 / マスタ管理 |
| 仕入を登録 | タイヤ・部材の仕入を最低限の項目で登録 | 仕入日 / 仕入先 / 商品名 / タイヤサイズ / 数量 / 単価 / 仕入合計 / 支払方法 / 備考 |
| 本日の状況 | 本日の業績スナップショット (6カード + 概算粗利益) | 売上 / 経費 / 仕入 / 粗利益(売上−経費−仕入) / 未確認 / 修正依頼 各件数 |
| 今月の状況 | 月初〜今日の累計 | 対象年月 / 月初日 / 本日 / 売上合計 / 経費合計 / 仕入合計 / 粗利益 / 売上件数 / 経費件数 / 仕入件数 / 未確認件数 / 修正依頼件数 |
| マスタ管理 (4タブ) | 顧客 / 商品 / 経費科目 / 支払方法 の閲覧・編集 | 顧客マスタ: 顧客名/電話/車種/車両番号/タイヤサイズ/住所/備考/最終来店日/累計売上 ／ 商品マスタ: 商品名/分類/タイヤサイズ/標準単価(売価)/標準仕入単価/在庫管理対象/備考 ／ 経費科目: 13項目 (表示のみ) ／ 支払方法: 7項目 (表示のみ) |

#### 店舗側入力項目と自動補完項目の分離（第1分割）

店舗側スマホ画面では、店長は最低限の項目だけを入力します。Google Sheets 仕様に合わせた詳細項目は **アプリ内部で自動補完** し、保存・送信時に整形します。

**売上 — 店舗側入力 / 自動補完:**
| 店舗側入力 | 自動補完 (アプリ側) |
|---|---|
| お客様名 / 売上区分 / 商品名 / タイヤサイズ / 数量 / 単価 / 合計金額 / 支払方法 / 車種 / 車両番号 / 作業内容 / 備考 | 登録ID / 登録日時 / 売上日 / 店舗名 / 担当者 / 販売者名 / 海外販売区分 / 確認ステータス / 本社確認者 / 本社確認日時 / 修正依頼有無 / 修正依頼内容 / 登録元 / Google Sheets送信状態 |

**経費 — 店舗側入力 / 自動補完:**
| 店舗側入力 | 自動補完 (アプリ側) |
|---|---|
| 経費日 / 購入先 / 内容 / 金額（税込） / 消費税 / 支払方法 / 経費科目 / レシート画像添付 / 備考 | 登録ID / 登録日時 / 店舗名 / 担当者 / AI分類科目 / 本社確定科目 / レシート画像URL / OCR読取結果 / 確認ステータス / 本社確認者 / 本社確認日時 / 修正依頼有無 / 修正依頼内容 / 登録元 (real/sample/memo/manual) / Google Sheets送信状態 |

**仕入 — 店舗側入力 / 自動補完:**
| 店舗側入力 | 自動補完 (アプリ側) |
|---|---|
| 仕入日 / 仕入先 / 商品名 / タイヤサイズ / 数量 / 単価 / 仕入合計 / 支払方法 / 備考 | 登録ID / 登録日時 / 店舗名 / 担当者 / 請求書番号 (本社) / 支払予定日 (本社) / 支払ステータス (本社・初期"未払") / 確認ステータス / 本社確認者 / 本社確認日時 / 修正依頼有無 / 修正依頼内容 / 登録元 ("store-purchase-upload") / Google Sheets送信状態 |

#### 顧客マスタの仕様（第1分割）

| 項目 | 説明 |
|---|---|
| `id` | `cust-*` の一意ID (自動採番) |
| `createdAt` | 登録日時 (ISO 8601) |
| `name` | 顧客名 (お客様名) |
| `phone` | 電話番号 |
| `carModel` / `carNumber` | 車種・車両番号 |
| `tireSize` | タイヤサイズ |
| `address` | 住所 |
| `note` | 備考 |
| `lastVisit` | 最終来店日 (売上登録時に自動更新) |
| `totalSales` | 累計売上 (売上登録時に自動加算) |
| `active` | 有効/無効フラグ |

- 売上登録の **お客様名** 欄は `<datalist>` で顧客マスタを候補表示
- 候補から選択した時、車種・車両番号・タイヤサイズが空欄の場合のみ自動補完
- 売上登録後、顧客が顧客マスタに **未登録の場合**、トーストで「マスタ管理から追加できます」と案内（**自動追加はしない** — 大量登録防止）
- localStorage キー: `xchange.customers.v1`
- 将来 Google Sheets **13_顧客マスタ** への送信に対応する構造

#### 商品マスタの仕様（第1分割）

| 項目 | 説明 |
|---|---|
| `id` | `prod-*` の一意ID |
| `name` | 商品名 |
| `category` | 分類 (例: タイヤ / 工賃 / 部材) |
| `tireSize` | タイヤサイズ |
| `unitPrice` | 標準単価 (売価) |
| `costPrice` | 標準仕入単価 |
| `stockManaged` | 在庫管理対象 (true/false) |
| `active` | 有効/無効 |
| `note` | 備考 |

- 仕入登録の **商品名** 欄は `<datalist>` で商品マスタを候補表示
- 候補から選択した時、タイヤサイズ・標準仕入単価が空欄の場合のみ自動補完
- 数量×単価から仕入合計を自動計算 (合計欄が空の場合のみ)
- ただし、確認画面で全て手修正可
- localStorage キー: `xchange.products.v1`

#### 経費科目マスタ・支払方法マスタ（第1分割）

**経費科目** 初期値 13項目: タイヤ仕入 / 工具消耗品 / 廃タイヤ処分費 / 店舗備品 / ガソリン代 / 車両関連費 / 通信費 / 広告宣伝費 / 修繕費 / 外注費 / **水道光熱費** (v3.18.0で追加) / 雑費 / その他

**支払方法** 初期値 7項目: 現金 / カード / QR決済 / 振込 / 売掛 / 引落 / その他

両マスタとも、現段階ではマスタ管理画面で **閲覧のみ** （`PAYMENT_METHODS_MASTER` / `EXPENSE_CATEGORIES` 定数で管理）。第3分割で Google Sheets と完全同期予定。

#### 第1分割で行わなかったこと

第1分割では以下は **未着手** です（第2・第3分割で対応予定、ただし既存実装は壊していません）:

- 本社確認ステータスの詳細操作（既存の「修正依頼」「確認済み処理」UI は維持）
- 修正依頼フローの完全実装（既存の再提出モーダルは維持）
- 決算用集計画面の完成（既存の本社「月次集計」は維持）
- 操作ログ画面（未実装）
- Google Sheets へのマスタ完全同期（マスタは localStorage のみ）

---

### 4-3a. 開発領域分割（3領域管理）

> 📌 **本プロトタイプは、今後の開発を分かりやすくするため、以下の3領域に分けて管理します。**
>
> - **A：売上音声入力・店舗側操作**
> - **B：経費OCR・AI分類**
> - **C：本社確認・月次集計・権限管理**
>
> 各領域を分けて管理することで、**経費OCR修正時に売上音声入力を壊さない、本社機能修正時に店舗機能を壊さない** など、保守性を高めます。

これまで売上音声入力 / 経費OCR / 本社確認 / 月次集計 / 権限管理予定 の機能が単一プロトタイプ内に積み重なってきました。今後の修正で他領域の既存機能を壊さないよう、3領域の境界・対象機能・修正方針を以下に明示します。

#### 領域A：売上音声入力・店舗側操作

**ステータス: 一旦完成済み扱い**（今後の経費OCR修正・本社機能修正で**壊さないこと**）

| 種別 | 対象機能 |
|---|---|
| 画面 | 店舗ダッシュボード / 売上音声入力 / 売上登録前確認 / 売上登録完了 / 本日売上一覧 / 修正依頼一覧 / 修正して再提出（売上） |
| 入力 | スマホ標準キーボード音声入力（Web Speech API 不要） / 音声テンプレート（12項目・項目名形式） |
| AI処理 | 売上音声テキスト→項目自動分解（自由文・構造化フィールド両対応） |
| データ | 売上レコード（type:"sale"）の作成 / 修正再提出（status:"未確認"へ戻す） |
| 主なファイル位置 | `app.js` の `parseVoiceText()` `parseStructuredFields()` `applyStructuredOverrides()` `setupVoiceUI()` `renderSalesConfirm()` `setupSalesConfirm()` `renderStoreDashboard()` `renderStoreTodaySales()` `renderStoreRejections()` |

#### 領域B：経費OCR・AI分類

**ステータス: 現在最も修正が必要な領域**（今後の修正では**領域A・Cを壊さないこと**）

| 種別 | 対象機能 |
|---|---|
| 画面 | 経費レシート登録 / 経費登録前確認 / 経費登録完了 / 本日経費一覧 |
| 入力 | レシート画像アップロード（背面カメラ起動）／ 画像プレビュー ／ 経費メモ入力 ／ サンプルレシート選択（デモ専用） |
| OCR処理 | 実OCR読取（Tesseract.js / ブラウザ内処理・APIキー不要）／ OCR進捗バー ／ OCR原文表示・編集 ／ サンプルOCR読取（固定テキスト）／ メモテキスト展開 |
| AI推定 | OCR結果から 購入先 / 金額（キーワード優先 7段階）/ 消費税 / 支払方法 / 日付 を抽出 ／ AI経費分類候補（Top3）／ 内容欄の短い要約生成 ／ 低信頼度時の手入力誘導 |
| データ | 経費レコード（type:"expense"）の作成 / `aiCategory`（AI分類スナップショット・immutable）/ `category`（店長/本社が変更可） / `ocrSource` ("real"/"sample"/"memo"/"empty") / `lowConfidence` フラグ |
| 主なファイル位置 | `app.js` の `extractAmountFromReceipt()` `extractTaxFromReceipt()` `extractVendorFromReceipt()` `extractPaymentFromReceipt()` `summarizeReceiptContent()` `parseExpenseText()` `classifyExpense()` `loadTesseract()` `runRealOCR()` `setupExpenseUpload()` `_buildDraftFromUpload()` `renderExpenseConfirm()` `setupExpenseConfirm()` |

#### 領域C：本社確認・月次集計・権限管理

**ステータス: 機能的には完成。本番化時に権限管理を実装予定**

| 種別 | 対象機能 |
|---|---|
| 画面 | 本社ダッシュボード（8KPI）/ 売上一覧 / 経費一覧 / 売上集計（売上レポート）/ 未確認一覧 / 月次集計 / CSV出力センター |
| 操作 | 詳細確認 / 確認済み処理 / 修正依頼送信 / 本社確定科目変更 / 月次締め処理（確認済み→月次処理済み）/ 月次締め解除 |
| 出力 | 売上CSV（15項目）/ 経費CSV（12項目）/ 月次集計CSV（サマリー10項目+全内訳+全明細）/ 期間指定CSV |
| 権限管理（本番化時） | ログイン認証 / 店舗ユーザー権限（店舗画面のみ）/ 本社ユーザー権限（本社画面）/ 管理者権限（全機能 + 月次締め解除） |
| 主なファイル位置 | `app.js` の `renderHQDashboard()` `renderHQSales()` `renderHQExpenses()` `renderHQPending()` `renderSalesReport()` `renderMonthly()` `exportSalesCSV()` `exportExpenseCSV()` `exportMonthlyCSV()` `exportSalesCSVByPeriod()` `setupRejectModal()` `setupCategoryChangeModal()` |

**現状の制限と本番化方針**

> 現在のプロトタイプでは、画面右上の「店舗 / 本社」切替ボタンで両画面を自由に行き来できます。これは**デモ確認のための仕様**であり、本番運用時には以下を実装します。
>
> - **ログイン認証**（Google ログイン or 店舗ID＋パスワード）
> - **店舗ユーザー** は本社画面（売上一覧・経費一覧・月次集計・CSV出力）にアクセスできない
> - **本社ユーザー** は店舗の音声入力・OCR入力にはアクセスできるが、店舗を限定して操作する
> - **管理者ユーザー** のみが月次締め解除・データ初期化・CSV一括出力を実行できる
> - 監査ログ（誰がいつ確認/修正/承認したか）の記録（電子帳簿保存法対応）

#### 領域間の依存関係

| 依存方向 | 内容 |
|---|---|
| A → 共通 | 売上レコード（`records[].type === "sale"`）は localStorage `xchange.records.v1` に書き込まれ、領域C の本社画面で参照される |
| B → 共通 | 経費レコード（`records[].type === "expense"`）は同じく localStorage に書き込まれ、領域C の本社画面・月次集計・CSV出力で参照される |
| C → A・B | 本社が「修正依頼」を発行すると、対象レコードの `status` が `"修正依頼"` になり、領域A（売上）/領域B（経費）の修正再提出フローで参照される |

データのスキーマ（売上 / 経費レコードの項目）は §6 データ構造 を**領域横断の契約**として扱います。スキーマ変更を伴う修正は、3領域すべての影響範囲を確認してから行います。

#### 修正時のチェックリスト

- ✅ **領域A の修正** → 領域B（経費OCR）と領域C（本社確認・CSV列構成）を破壊していないか確認
- ✅ **領域B の修正** → 領域A（売上音声入力）の `parseVoiceText` / `parseStructuredFields` に手を加えていないか、本社の経費一覧・月次集計の表示が崩れていないか確認
- ✅ **領域C の修正** → 領域A・B のデータ書き込みに後方互換性があるか（`migrateRecord()` のステータス名対応）、CSV列順を変える場合は会計ソフト連携先への通知が必要か確認
- ✅ **共通スキーマ変更** → §6 データ構造の更新 + `migrateRecord()` への移行ロジック追加 + 領域A・B・C 全画面でのレンダリング確認

---

## 5. 画面一覧

### 店舗側（スマートフォン最適化）

| ID | 画面名 | 概要 |
|---|---|---|
| S1 | 店舗ダッシュボード | 本日のサマリー + 業務メニュー（5大ボタン）+ 修正依頼バナー |
| S2 | 売上 音声入力画面 | マイク録音／サンプル投入／編集可能テキスト＋AIで分解ボタン |
| S3 | 売上 登録確認画面 | 大型サマリー(合計・数量・単価) + 全項目編集可 |
| S4 | 売上 登録完了画面 | 完了表示 + 「続けて入力」ボタン |
| S5 | 経費 レシート登録画面 | 画像アップロード + メモ + OCR読取 + AI分類ボタン |
| S6 | 経費 登録確認画面 | 大型サマリー + AI分類候補 + 全項目編集可 |
| S7 | 経費 登録完了画面 | 完了表示 + 「続けて入力」ボタン |
| S8 | 本日の売上一覧画面 | 当日売上合計と件別カード |
| S9 | 本日の経費一覧画面 | 当日経費合計と件別カード |
| S10 | 修正依頼一覧画面 | 本社からの修正依頼カード（詳細／修正／再提出ボタン） |

### 本社側（PC最適化）

| ID | 画面名 | 概要 |
|---|---|---|
| H1 | 本社ダッシュボード | 8 KPIカード + 未確認の登録一覧 + 本日の登録一覧 |
| H2 | 売上一覧 | 11列テーブル（日付・店舗・売上区分・商品・サイズ・数量・合計・支払・担当・ステータス・操作） |
| H3 | 経費一覧 | 11列テーブル（日付・店舗・購入先・内容・金額・AI分類・本社確定・支払・ステータス・レシート・操作） |
| H4 | 未確認一覧 | 売上/経費混在の未確認データ、タブ切替・カード上ワンタップ承認 |
| H5 | 月次集計画面 | 月次サマリー + 大型KPI3 + ステータス4 + 売上内訳4 + 経費内訳3 + 月次締めボタン |
| H6 | CSV出力センター | 売上CSV / 経費CSV / 月次集計CSV を期間指定で出力 |

### 共通（モーダル）

| 名称 | 用途 |
|---|---|
| 詳細確認モーダル | 売上/経費レコードの全項目表示。経費はレシート画像も含む |
| 修正依頼モーダル | 本社が店舗へ修正コメント付きで差し戻す |
| 本社確定科目 変更モーダル | 経費の本社確定科目を 12 カテゴリから変更（本社のみ） |
| レシート画像 拡大モーダル | レシート画像を拡大表示（アップロード/サンプル/画像なしを判別） |
| 修正・再提出モーダル | 店舗が修正依頼を受けたデータを編集して再提出（売上/経費別UI） |

---

## 6. データ構造

### 6-1. 売上データ

| 項目 | 内容 / 例 |
|---|---|
| 日付 | 売上日 (YYYY-MM-DD) |
| 店舗名 | 既定: 吉田自動車工業 X-Change本店 |
| 担当者 | 既定: 店長 |
| 売上区分 | 9種から複数選択 (新品タイヤ販売 / 中古タイヤ販売 / タイヤ交換工賃 / バランス調整 / 廃タイヤ処分料 / バルブ交換 / パンク修理 / ホイール販売 / その他) |
| 商品名 | 例: ブリヂストン レグノ GR-XII |
| タイヤサイズ | 例: 195/65R15 |
| 数量 | タイヤ本数 等 |
| 単価 | 円 (税込) |
| 合計金額 | 円 (税込) |
| 支払方法 | 現金 / クレジットカード / QR決済 / 銀行振込 / 売掛 / その他 |
| 車種 | 例: プリウス (40車種マスタから自動マッチ) |
| 車両番号 | 例: 品川 300 あ 12-34 |
| 作業内容 | 自由記述 |
| 備考 | 自由記述 |
| 確認ステータス | 未確認 / 確認済み / 修正依頼 / 月次処理済み |

### 6-2. 経費データ

| 項目 | 内容 / 例 |
|---|---|
| 日付 | 経費発生日 (YYYY-MM-DD) |
| 店舗名 | 既定: 吉田自動車工業 X-Change本店 |
| 担当者 | 既定: 店長 |
| 購入先 | 例: ENEOS 田中SS / カインズ |
| 内容 | 例: ガソリン代（送迎車給油） |
| 金額 | 円 (税込) |
| 消費税 | 10% 逆算 |
| 支払方法 | 現金 / クレジットカード / 銀行振込 / 口座引落 / その他 |
| AI分類科目 | AIが登録時に提案した科目 (immutable・監査用) |
| 本社確定科目 | 本社が最終確定する科目（確認済み/月次処理済み 時に有効） |
| レシート画像 | アップロード画像 / サンプル絵文字 / 画像なし |
| 備考 | 自由記述 |
| 確認ステータス | 未確認 / 確認済み / 修正依頼 / 月次処理済み |

経費科目（12カテゴリ）: タイヤ仕入 / 工具消耗品 / 廃タイヤ処分費 / 店舗備品 / ガソリン代 / 車両関連費 / 通信費 / 広告宣伝費 / 修繕費 / 外注費 / 雑費 / その他

---

## 7. 導入効果

| 効果 | 内容 |
|---|---|
| **売上報告遅延の削減** | 音声入力 + AI分解で1件あたり数十秒で登録完了。本社へ即時共有 |
| **経費精算遅延の削減** | レシート撮影 + OCR + AI分類で月末まとめ作業が不要に |
| **レシート提出漏れ防止** | 当日中の登録が習慣化、未提出レシートを残さない |
| **本社確認作業の効率化** | 売上一覧・経費一覧・未確認一覧で一括レビュー、ワンタップで確認済み |
| **修正依頼の可視化** | 店舗ダッシュボードに件数バッジと赤バナー、修正依頼コメントが明確 |
| **月次決算の早期化** | 月内のデータが日々確認済みになるため、月初の確定が当日中に可能 |
| **タイヤ販売部門の利益可視化** | 売上 − 経費 = 粗利益概算をリアルタイムに確認 |
| **店長の手入力負担削減** | キーボード入力から音声入力 / 画像アップロードへ移行 |
| **デジタル管理への移行** | 紙・Excel中心の運用からクラウド型運用へ |
| **情報共有スピード向上** | 本社・店舗間のタイムラグがほぼゼロに |

---

## 8. 本番化に必要な追加開発

| 項目 | 内容 |
|---|---|
| 実音声認識 | ブラウザ Web Speech API もしくは Whisper / Google STT / Azure Speech 連携 |
| 高精度OCR | プロトタイプの Tesseract.js（ブラウザ内・APIキー不要）から、**Google Cloud Vision OCR** または **Document AI（Receipt Parser）** へ切替（精度・構造化抽出が大幅向上、詳細は §11-4） |
| Google Sheets 連携 | 売上 / 経費 / 月次集計を Google Sheets に自動書き込み |
| Google Drive 連携 | レシート画像を Google Drive に保存（フォルダ自動分類） |
| ログイン認証 | Google ログイン or 店舗ID＋パスワードによる認証 |
| 権限管理 | 店長 / 本社事務 / 管理者 のロール別アクセス制御 |
| データバックアップ | DB自動バックアップ / 復元手順 / レシート画像の冗長保管 |
| スマホ実機テスト | iOS / Android 主要機種で動作・カメラ・音声入力検証 |
| 本番サーバー環境 | クラウド (GCP / AWS / Azure) / HTTPS / WAF / CDN |
| セキュリティ対策 | 個人情報保護対応 / 暗号化（保管時・通信時）/ 脆弱性対策 |
| 操作ログ管理 | 監査ログ / 改竄検知（電子帳簿保存法対応） |

---

## 9. 補助金申請で説明できるポイント

| ポイント | 内容 |
|---|---|
| **AIによる売上項目分解** | 店長の発話を AI が 商品名・タイヤサイズ・数量・単価・合計・支払方法・車種・売上区分 に自動分解。店長の手入力負担を大幅に削減 |
| **OCRによる経費レシート読取** | レシート画像から 日付・購入先・金額・消費税・支払方法・内容 を自動抽出 |
| **AIによる経費科目分類** | 12 カテゴリから Top3 候補をAIが提示、店長が選択するだけで分類完了 |
| **店舗・本社のリアルタイム共有** | 店舗での登録が本社側に即時反映、ロール切替なしで両者の画面が同期 |
| **本社確認・承認フローのデジタル化** | 紙・メール・電話のやり取りを撤廃、画面上で 確認 / 修正依頼 / 月次締め まで完結 |
| **月次集計とCSV出力による会計処理効率化** | 月次集計CSVを会計ソフト（freee / マネーフォワード / 弥生）に取り込み可能 |
| **業務時間削減** | 店舗側: 売上報告・経費精算 / 本社側: 確認作業・月次決算 の各工程が短縮 |
| **経営状況の可視化** | 8KPIダッシュボード / 月次集計 / カテゴリ別内訳 / 商品別 Top10 を即座に把握 |
| **小規模店舗でも使えるAI業務改善システム** | 店長1名運営の小規模店舗でも、本格的な AI / OCR の恩恵を受けられる |

---

## 10. 画面上の説明文（スクリーンショット用）

各主要画面に、補助金申請資料の **スクリーンショット用** 説明文（青系の `.biz-note` バナー）を配置しています。

| 画面 | 画面上に表示される説明文 |
|---|---|
| 店舗ダッシュボード (S1) | 📡 店舗で登録された売上・経費は、本社側に **即時反映** されます |
| 売上 音声入力画面 (S2) | 🎤 マイク録音またはサンプル文の編集で、**AIが売上項目を自動分解** します |
| 売上 登録確認画面 (S3) | 🤖 AIが分解しました。**必ず内容をご確認ください**（既存のAIバナー） |
| 経費 レシート登録画面 (S5) | 📷 レシート画像の **OCR読取** と **AIによる経費科目分類** で入力作業を効率化します |
| 経費 登録確認画面 (S6) | 🤖 AIが分類しました。**必ず内容をご確認ください**（既存のAIバナー） |
| 修正依頼一覧画面 (S10) | 本社から修正依頼が届いています。内容を確認し、必要な項目を修正して再提出してください |
| 本社ダッシュボード (H1) | 🏢 本社側で売上・経費の **確認・修正依頼・月次締め** 処理を一元管理します |
| 売上一覧 (H2) | 📋 店長が登録した売上を本社で **確認・承認** します |
| 経費一覧 (H3) | 🧾 経費の確認と **AI分類科目から本社確定科目への変更** ができます |
| 未確認一覧 (H4) | 店長が登録した未確認データを 売上 / 経費 タブで一括処理できます |
| 月次集計画面 (H5) | 📊 月単位で **売上・経費・粗利益** を集計、月次締めで会計確定処理ができます |
| CSV出力画面 (H6) | 会計ソフト連携・補助金申請資料用にCSVをダウンロードできます |

これらの説明文は、補助金申請担当者がスクリーンショットを撮ったとき、その画面が何の機能を表しているか **一目で分かる** ように配置されています。

---

### 10-1. Google スプレッドシート連携（v3.17.4 新規 / v3.17.5 拡張）

> 📡 **売上・経費・仕入の3種類に対応**
> 「確認して登録」を押すと、既存の localStorage 保存に加え、Google Apps Script Web アプリ経由で Google スプレッドシートにも 1行追記されます。送信に失敗した場合でも、**ローカル保存は維持され、`sheetsSyncStatus` に `failed` が記録されます**。

| action | 送信先シート | 呼び出し元 | ステータス |
|---|---|---|---|
| `addSale`     | **01_売上明細** | 売上 登録前確認 →「確認して登録」 | ✅ 稼働中（v3.17.4） |
| `addExpense`  | **02_経費明細** | 経費 登録前確認 →「この内容で登録」 | ✅ 稼働中（v3.17.5） |
| `addPurchase` | **03_仕入明細** | 仕入 登録前確認（**専用画面未実装**） | 🟡 ヘルパ準備済 / 将来有効化 |

> 💡 **仕入登録画面について**
> 現プロトタイプには専用の仕入登録画面はなく、タイヤ仕入は「経費レシート登録」フローで `category: "タイヤ仕入"` として記録し **02_経費明細** へ送信されます。`sendPurchaseToSheets()` ヘルパ関数は実装済みで、将来 `type:"purchase"` 専用画面（商品名・タイヤサイズ・数量・単価・請求書番号・支払予定日・支払ステータス入力欄）を追加した際にそのまま呼び出せます。

#### 設定

`app.js` 冒頭の `XCHANGE_SHEETS_CONFIG` で制御します。

```javascript
const XCHANGE_SHEETS_CONFIG = {
  enabled: true,
  endpoint: 'https://script.google.com/macros/s/.../exec',
  token:    'XCHANGE_TOKEN_2026'
};
```

- `enabled: false` で送信を無効化（ローカル保存のみで動作）
- `endpoint` は Apps Script ウェブアプリの公開URL（アクセスは「全員」「自分として実行」推奨）
- `token` は Apps Script 側 `doPost(e)` で照合する共有秘密

#### Apps Script 側で受け取る JSON

**売上 (action: addSale → 01_売上明細)** — 18項目
```json
{
  "action": "addSale",
  "token":  "XCHANGE_TOKEN_2026",
  "payload": {
    "saleDate":      "2026-05-18",
    "storeName":     "吉田自動車工業 X-Change本店",
    "staffName":     "店長",
    "customerName":  "井上様",
    "sellerName":    "店長",
    "saleCategory":  "新品タイヤ販売,タイヤ交換工賃",
    "productName":   "ヨコハマ ブルーアース",
    "tireSize":      "185/65R15",
    "quantity":      4,
    "unitPrice":     13500,
    "totalAmount":   54000,
    "paymentMethod": "現金",
    "carModel":      "ノート",
    "carNumber":     "品川 500 さ 22-22",
    "workContent":   "新品タイヤ販売、バランス調整",
    "memo":          "",
    "overseasSale":  "",
    "confirmStatus": "未確認"
  }
}
```

**経費 (action: addExpense → 02_経費明細)** — 16項目
```json
{
  "action": "addExpense",
  "token":  "XCHANGE_TOKEN_2026",
  "payload": {
    "expenseDate":        "2026-05-18",
    "storeName":          "吉田自動車工業 X-Change本店",
    "staffName":          "店長",
    "vendor":             "ENEOS 田中SS",
    "content":            "ガソリン代",
    "amount":             5000,
    "tax":                454,
    "paymentMethod":      "現金",
    "aiCategory":         "ガソリン代",
    "hqCategory":         "ガソリン代",
    "receiptImageUrl":    "data:image/jpeg;base64,...",
    "ocrText":            "[実OCR読取 (Tesseract.js)]\n...",
    "memo":               "",
    "confirmStatus":      "未確認",
    "hasRevisionRequest": "なし",
    "source":             "real"
  }
}
```

**仕入 (action: addPurchase → 03_仕入明細)** — 15項目（専用画面未実装・ヘルパ準備済）
```json
{
  "action": "addPurchase",
  "token":  "XCHANGE_TOKEN_2026",
  "payload": {
    "purchaseDate":    "2026-05-18",
    "storeName":       "吉田自動車工業 X-Change本店",
    "staffName":       "店長",
    "vendor":          "ブリヂストンタイヤジャパン",
    "productName":     "レグノGR-XII",
    "tireSize":        "195/65R15",
    "quantity":        24,
    "unitPrice":       7716,
    "totalAmount":     185000,
    "paymentMethod":   "銀行振込",
    "invoiceNo":       "A-2026-05003",
    "paymentDueDate":  "2026-06-30",
    "paymentStatus":   "未払",
    "memo":            "",
    "source":          "store-purchase-upload"
  }
}
```

CORS preflight を避けるため `Content-Type: text/plain;charset=utf-8` で送信します。Apps Script 側は `JSON.parse(e.postData.contents)` で受け取れます。

#### 送信状況の追跡（`sheetsSyncStatus`）

各売上 / 経費 / 仕入 レコードに同期状況フラグが付与されます。

| 状態 | 意味 |
|---|---|
| `pending` | 未送信（登録直後の初期値） |
| `synced`  | 送信成功（Apps Script から 200 応答） |
| `failed`  | 送信失敗（オフライン / エンドポイント未設定 / トークン不一致 等。`sheetsSyncError` に失敗理由を保存） |

#### 画面表示

| 種別 | 送信成功 | 送信失敗 |
|---|---|---|
| 売上 | `Googleスプレッドシートへ登録しました` | `Googleスプレッドシートへの送信に失敗しました。ローカルには登録されています。` |
| 経費 | `Googleスプレッドシートへ経費を登録しました` | `Googleスプレッドシートへの経費送信に失敗しました。ローカルには登録されています。` |
| 仕入 | `Googleスプレッドシートへ仕入を登録しました` | `Googleスプレッドシートへの仕入送信に失敗しました。ローカルには登録されています。` |

いずれもユーザーは登録完了画面を見ており、非同期で送信が進行します。

#### 経費金額の優先順位（v3.17.6 確立）

> ⚠ **最終確認画面の入力欄が常に最優先**
> 経費の `amount` は、localStorage 保存・Google Sheets 送信のいずれも **`finalExpenseRecord.amount`** （登録ボタン押下時点の `#confExpenseAmount` 入力欄の値）を参照します。OCR推定値・AI分類・サンプル経費・直前の `draftExpense.amount` で上書きされません。

優先順位（経費登録時）:

1. **最終確認画面の金額入力欄** に現在入っている値（`#confExpenseAmount`）← 常にこれが採用される
2. 経費入力画面でユーザーが手入力したメモ値（`parseExpenseText` 経由）
3. OCR / AI分類で推定された値（`extractAmountFromReceipt`）
4. サンプル経費の値（`SAMPLE_RECEIPTS[].amount`、サンプル選択時のみ）

入力形式は全て正規化されます: `3000` `3,000` `￥3000` `¥3,000` `１５０００` `15,000円` → いずれも `3000` または `15000` として保存。

#### 既存機能への影響

> ⚠ **送信失敗時も既存機能は壊れません。**
> Sheets 送信は fire-and-forget で非同期実行され、結果は `sheetsSyncStatus` フラグに記録されるのみです。localStorage 保存・店舗側売上一覧・店舗側経費一覧・本社側売上一覧・本社側経費一覧・月次集計・CSV出力・OCR画面・AI分類画面・スマホ表示は **全て通常通り動作** します。

#### 本番化時の拡張

- 仕入専用画面（type:"purchase"）の新設と `sendPurchaseToSheets()` の有効化
- 確認/修正/月次締めイベントの送信（`action: "updateStatus"`）
- 失敗時の自動リトライキュー（オフライン時の蓄積送信）
- Apps Script でのトークン検証 + ログ記録 + Drive へのレシート画像保管

---

## 11. クイックスタート（プロトタイプ動作確認）

```bash
# このフォルダを開いて index.html をブラウザで開くだけ
open index.html              # macOS

# もしくは簡易ローカルサーバ
python3 -m http.server 8080
# → http://localhost:8080/
```

**動作確認推奨環境:**
- 店舗側: スマートフォン or Chrome DevTools のモバイルエミュレータ（375×812 等）
- 本社側: PC ブラウザ幅 1280px 以上（左サイドバー + テーブル一覧）
- 補助金申請資料用スクリーンショット: PC 全幅 1280px 以上での撮影を推奨

**プロトタイプの特徴:**
- データは `localStorage` に保存され、ブラウザを閉じても保持
- フッター 「**デモデータ初期化**」 でいつでも初期状態に戻せる
- 画面右上の 「店舗 / 本社」 で同じデータを別UIから確認可能（即時反映）
- 依存ライブラリなし。素のHTML/CSS/JSのみで動作

### 11-1. スマートフォン表示対応（v3.9 強化）

本プロトタイプは **スマホ・タブレット・PC のすべてに対応** しています。

| 利用シーン | 想定デバイス | 表示の特徴 |
|---|---|---|
| **店舗側** | **スマートフォン操作を想定** （iPhone / Android, 360〜414px 縦向き） | KPIカード・大ボタン・モーダルすべてがスマホ片手操作に最適化 |
| **本社側** | **PC操作を想定** （1280px〜）/ ただし **スマホでも確認可能** | PCではテーブル一覧、スマホでは自動でカード表示に切り替え |

**スマホ表示時の主な対応:**
- **横はみ出し完全防止** (`body { overflow-x: hidden }`)
- 上部「店舗／本社」切替ボタンが 360px 級画面でも崩れない（ブランド副題は自動で省略）
- 売上一覧／経費一覧のテーブル → **モバイルで自動的にラベル付きカード表示** に変換
- 全モーダル → スマホで `max-width: 95vw` に収まる
- 月次集計画面のKPIカード／内訳カード／月次締めボタンを縦並び化
- 店舗ダッシュボードの 5 KPI を 2列グリッド + 5枚目全幅レイアウトに変更
- 録音マイクボタン・大型サマリー・アクションボタンの縮小調整
- 長文セルは省略表示（`title` 属性でホバー全文確認可能）
- iPhone 390px / Android 360-412px の縦向きで右端切れなし

### 11-2. 売上音声入力 — スマホ標準キーボードのマイクを利用（v3.11 / v3.13 / v3.14 強化）

> 📌 **音声入力の話し方ガイド (v3.14)**
>
> 売上音声入力は、**番号を読み上げる必要はありません**。項目名に続けて短く区切って話すことで、AI分解の精度が上がります。
> **車両番号は音声認識で誤認識しやすいため、登録確認画面で必ず確認・修正してください。**

#### おすすめの話し方（番号なし・項目名形式）

各項目を **「項目名 → 区切り → 値」** の形式で短く区切って話します:

```
売上区分:新品タイヤ販売
商品名:ヨコハマ アイスガード
タイヤサイズ:195/65R15
数量:4本
車種:プリウス
車両番号:品川300あ1234
合計金額:68,000円
支払方法:現金
作業内容:タイヤ交換、バランス調整、廃タイヤ処分あり
備考:高橋様
```

3種類の入力ボタンを用意しています:

| ボタン | 動作 |
|---|---|
| 📱 **スマホ音声入力を使う** | テキスト欄にフォーカス → スマホキーボード起動 → マイク🎤で音声入力 |
| 📋 **サンプル音声を入れる** | デモ用の完全な音声サンプル文を投入（全項目入り） |
| 📝 **入力テンプレートを入れる** | 項目名のみのスケルトンを投入 → 各項目に音声入力で値を埋める |
| クリア | テキスト欄を空にする |

#### AI分解が対応する表現パターン（v3.14）

`parseStructuredFields()` で以下のラベル + 区切り を認識して各フィールドに自動振り分けます:

| 認識する項目名 | 振り分け先 |
|---|---|
| 売上区分: / 売上区分、 | salesCategories |
| 商品名: / 商品名、 | productName |
| タイヤサイズ: / サイズ: / 〜、 | tireSize |
| 数量: / 数量、 | qty |
| 車種: / 車種、 | carModel |
| 車両番号: / ナンバー: / 〜、 | carNumber |
| 合計金額: / 金額: / 〜、 | total |
| 支払方法: / 支払い: / 支払: / 〜、 | paymentMethod |
| 作業内容: / 作業内容、 | workContent |
| 備考: / 備考、 | note |
| お客様: / お客様名: / 〜、 | customer |

区切り文字は **「:」「：」「、」「,」** のいずれにも対応。値は次のラベル/「。」/改行/末尾まで。

**自由文章形式（ラベルなし）も従来通り分解可能:**
> 「新品タイヤ4本、195/65R15、プリウス、合計48,000円、現金、廃タイヤ処分あり」 → 既存の正規表現パーサで処理

#### 入力テンプレート（v3.15: 12項目・日付/店舗/担当者は除外）

> 📌 **売上音声入力では、日付・店舗名・担当者はシステム側でデフォルト表示します。**
> 店長は、売上ごとに変わるお客様名、商品、金額、支払方法、車両情報、作業内容などを音声入力し、
> 登録前確認画面で全項目を最終確認・修正できます。

**売上音声入力画面を開いた時点 / 「📝 入力テンプレートを入れる」ボタン / 「クリア」ボタン** いずれの場合も、以下の **12項目テンプレート** が初期表示されます:

```
お客様名:
売上区分:
商品名:
タイヤサイズ:
数量:
単価:
合計金額:
支払方法:
車種:
車両番号:
作業内容:
備考:
```

各行をタップしてカーソルを置き、スマホキーボードのマイクボタンで該当項目だけ短く話す → 音声認識精度が上がる、という流れです。

#### 自動設定されるデフォルト値（音声入力テンプレートからは除外）

下記3項目はシステム側で自動設定され、音声入力テンプレートには含まれません。登録確認画面では引き続き編集可能です。

| 項目 | デフォルト値 |
|---|---|
| 日付 | 本日の日付 |
| 店舗名 | 吉田自動車工業 X-Change本店 |
| 担当者 | 店長 |

#### 登録確認画面で編集可能な全15項目

音声入力した12項目 + 自動設定3項目 = 合計**15項目すべて編集可能**:

1. 日付（自動・編集可）
2. 店舗名（自動・編集可）
3. 担当者（自動・編集可）
4. お客様名（音声入力）
5. 売上区分（音声入力）
6. 商品名（音声入力）
7. タイヤサイズ（音声入力）
8. 数量（音声入力）
9. 単価（音声入力）
10. 合計金額（音声入力）
11. 支払方法（音声入力）
12. 車種（音声入力）
13. 車両番号（音声入力・**空欄でも登録可**）
14. 作業内容（音声入力）
15. 備考（音声入力）

> 📌 **音声入力 → AI分解 → 確認画面 の方針**
>
> 本システムでは、売上内容をスマートフォン標準音声入力でまとめて入力できます。
> AIが項目ごとに分解しますが、**音声認識やAI分解には誤認識の可能性があるため、登録前に必ず確認画面を表示し、全項目を修正できる仕様**としています。
> 特に **金額・数量・支払方法・タイヤサイズ・車両番号** は確認が必要です。

#### 音声入力できる全項目（14項目）

下記は、音声でまとめて発話 → AIが自動分解 → 確認画面で **全項目編集可能** な項目です。

| 項目 | 音声例 | 確認画面での扱い |
|---|---|---|
| 日付 | (自動: 今日) | 編集可（日付ピッカー） |
| 店舗名 | (自動: 既定値) | 編集可 |
| 担当者 | (自動: 既定値) | 編集可 |
| 売上区分 | 「売上区分、新品タイヤ販売」 | 9種チップ（複数選択可） |
| 商品名 | 「商品名、ヨコハマ アイスガード」 | 編集可 |
| **タイヤサイズ** ⚠️ | 「タイヤサイズ、195 65 R15」 | **要確認タグ付き**で編集可 |
| 数量 ⚠️ | 「数量、4本」 | **大型入力欄＋⚠️**で編集可 |
| 単価 ⚠️ | (自動: 合計÷数量) | **大型入力欄＋⚠️**で編集可 |
| 合計金額 ⚠️ | 「合計金額、6万8千円」 | **大型入力欄＋⚠️ 要確認**で編集可 |
| **支払方法** ⚠️ | 「支払方法、現金」 | **6タイル＋⚠️ 要確認**で選択 |
| 車種 | 「車種、プリウス」 | 編集可 |
| **車両番号** ⚠️ | 「車両番号、品川 300 あ 1234」 | **⚠️ 要確認＋手入力推奨の注意書き**付き |
| 作業内容 | 「作業内容、タイヤ交換、バランス調整、廃タイヤ処分あり」 | 編集可 |
| 備考 | 「備考、高橋様」 | 編集可 |

#### 完全な音声入力例（v3.13でサンプル投入できるテキスト）

> 「売上区分、新品タイヤ販売。商品名、ヨコハマ アイスガード。タイヤサイズ、195 65 R15。数量、4本。車種、プリウス。車両番号、品川 300 あ 1234。合計金額、6万8千円。支払方法、現金。作業内容、タイヤ交換、バランス調整、廃タイヤ処分あり。備考、高橋様。」

→ 「📋 サンプル音声を入れる」ボタンで上記がテキストエリアに投入され、「🤖 AIで分解する」 で確認画面へ遷移します。

#### 確認画面（売上 登録前確認）の仕様

- **画面上部の注意バナー（赤系）**: 「🤖 音声入力の内容をAIが分解しました。登録前に、金額・数量・支払方法・車両番号を必ず確認してください。」
- **大型サマリー（青グラデ）**: 合計金額（38px）/ 数量 / 単価 すべてに ⚠️タグ付き
- **支払方法**: 6タイル選択（⚠️ 要確認 タグ）
- **タイヤサイズ・車両番号**: 個別フィールドラベルに ⚠️ 要確認 タグ
- **車両番号**: 注意書き「車両番号は音声入力で間違いやすいため、必要に応じて手入力で修正してください。空欄のままでも登録できます。」
- **アクションボタン**: 「🎤 音声入力へ戻る」 / 「✓ 確認して登録」
- 「🎤 音声入力へ戻る」を押すと音声入力画面に戻り、元の音声テキストを再編集できる

#### 「✓ 確認して登録」ボタンの動作

- 押すとデータが店舗側 [本日売上一覧] / 本社側 [売上一覧] / [未確認一覧] に **即時反映**
- 確認ステータスは「未確認」で登録される
- バリデーション: 合計金額・数量・支払方法・売上区分（1つ以上）が必須
- 車両番号は空欄でも登録可



> 💡 **本システムの売上音声入力は、スマートフォン標準キーボードの音声入力を利用します。**
> **外部音声認識APIは使用しないため、音声認識API料金は発生しません。**
>
> ⚠️ **iPhoneの場合**: 「設定」 > 「一般」 > 「キーボード」 > 「**音声入力を有効にする**」 が **ON** になっている必要があります。
>
> ⚠️ **Androidの場合**: Gboard 等の標準キーボードに音声入力ボタン（🎤）が表示されていることをご確認ください。

売上音声入力画面（[S2]）は、**スマホのキーボードに標準搭載されているマイクボタン** を使った音声入力をメイン導線として設計しています。

#### 利用フロー（実機スマホで操作する場合）

1. 店舗ダッシュボード → 「🎤 音声で売上登録」
2. 画面上部の案内：「📱 下の入力欄をタップするとスマホのキーボードが開きます。キーボード内のマイクボタン 🎤 を押して売上内容を話してください。」
3. 入力例を確認:「新品タイヤ4本、195/65R15、プリウス、合計48,000円、現金、廃タイヤ処分あり、バランス調整あり」
4. **「📱 スマホ音声入力を使う」ボタン** をタップ、または **大きなテキスト欄を直接タップ**
5. スマホのキーボードが立ち上がるので、**キーボード上のマイクアイコン 🎤** を押す
6. 売上内容を話す → スマホ標準音声認識でテキストに変換 → 入力欄に自動的に文字が入る
7. 入力されたテキストを画面で確認・必要なら手入力で修正
8. 下部の「🤖 AIで分解する」ボタンを押す → 確認画面で項目別表示・編集 → 登録

#### 設計の利点（API料金ゼロの仕組み）

| 項目 | 内容 |
|---|---|
| **音声認識API料金** | **¥0**（端末標準の Google 音声入力 / iOS Siri 音声入力を使用） |
| **APIキー管理** | **不要**（外部API接続なし） |
| **追加SDK・ライブラリ** | **不要**（HTML 標準の `<textarea>` のみ） |
| **個人情報のサーバー送信** | **発生なし**（音声データは端末→OSの音声認識→テキスト返却で完結） |
| **オフライン対応** | スマホOSの音声認識が対応している範囲で利用可（OS依存） |

#### テキストエリアの属性

| 属性 | 値 | 効果 |
|---|---|---|
| `lang` | `"ja-JP"` | 日本語入力を明示、音声認識の精度向上 |
| `inputmode` | `"text"` | 標準キーボードを呼び出す（マイクボタン付き） |
| `enterkeyhint` | `"done"` | モバイルキーボードの確定キーを「完了」表示 |
| `autocomplete` | `"off"` | 自動補完を無効化（音声入力に集中） |
| `font-size` | `16px` | iOS Safari の自動ズームを防止 |
| `rows` | `8` | 大型表示で誤タップ防止 |

#### 主要ボタン (v3.11)

| ボタン | 役割 |
|---|---|
| **📱 スマホ音声入力を使う** | テキスト欄にフォーカス → スマホキーボードを開く（メイン導線） |
| 📋 サンプル音声を入れる | プロトタイプ用にサンプル文を投入（デモ・PC操作時） |
| クリア | テキスト欄を空にする |
| 🤖 AIで分解する | 入力テキストを売上項目に自動分解 → 確認画面へ |

> **重要**: 本システムは **Web Speech API のブラウザ録音機能を使いません**。録音処理・音声データ処理は **本アプリ側では一切行わない** ため、API料金やプライバシー懸念が発生しない設計です。スマホOSが提供する音声入力機能をそのまま活用します。
>
> **v3.11 で削除した機能**: 大型「録音開始」ボタン（モック）→ スマホ音声入力ボタンに置換

### 11-3. 売上集計画面（v3.16 新規）

> 📌 **売上を 当日・当月・全期間 で多角的に集計**
> 本社が売上を **日別・月別・支払方法別・売上区分別・商品別・タイヤサイズ別** に確認・CSV出力できます。

#### 確認できる売上指標（KPI）

本社サイドバーの **「📈 売上集計」** から開けます。画面上部に以下の指標が並びます。

**期間別 売上 (5カード)**
- **本日売上** / 件数
- **昨日売上** / 件数
- **当月売上** / 件数
- **前月売上** / 件数
- **全期間売上合計** / 件数

**件数 (4カード)**
- 本日売上件数
- 当月売上件数
- 未確認売上件数
- 確認済み売上件数

#### 期間別・カテゴリ別の集計

| 集計種別 | 内容 |
|---|---|
| **日別売上** | 絞り込み期間内の日付ごとの合計 + 件数 |
| **月別売上** | 全期間の月ごとの合計 + 件数 |
| **支払方法別** | 現金 / カード / QR / 振込 / 売掛 / その他 別の合計 |
| **売上区分別** | 9種カテゴリ別の合計（複数選択時は等分配分） |
| **商品別 Top10** | 商品名別の合計（売上上位10件） |
| **タイヤサイズ別 Top10** | タイヤサイズ別の合計（売上上位10件） |

#### 絞り込み機能

5つのフィルタを組み合わせて、リアルタイムに集計結果を更新します:

| フィルタ | 値 |
|---|---|
| 開始日 | 任意の日付（既定: 当月1日）|
| 終了日 | 任意の日付（既定: 本日）|
| 支払方法 | 全て / 現金 / クレジットカード / QR決済 / 銀行振込 / 売掛 / その他 |
| 売上区分 | 全て / 9種から1つ |
| 確認ステータス | 全て / 未確認 / 確認済み / 修正依頼 / 月次処理済み |

「🔄 リセット」で当月＋全項目「全て」に戻せます。

#### クイックリンク（他画面への移動）

- **📋 売上一覧画面へ** — 売上一覧タブへ移動
- **📅 当月のみで絞り込む** — フィルタを当月範囲にセット（同画面）
- **⏳ 未確認売上一覧へ** — 未確認一覧タブの売上だけを表示

#### CSV出力（期間指定）

「📥 期間CSV出力」ボタンで、絞り込み中の売上データをCSV出力できます。
- 列構成: 既存の売上CSVと同じ（15項目）
- ファイル名に **対象期間** を入れる
  - 例: `xchange_sales_2026-05-01_2026-05-31.csv`

#### 店舗ダッシュボードの簡易売上集計

店舗側のダッシュボードにも、店長が売上を把握できる **6カードレイアウト** で表示:

| カード | 内容 |
|---|---|
| 本日売上 | 当日売上額 + 件数 |
| **当月売上** （v3.16 新規）| 当月累計 + 件数 |
| 本日経費 | 当日経費額 + 件数 |
| 本日粗利益 | 当日 売上 − 経費 |
| 未確認 | 未確認登録の合計件数 |
| 修正依頼 | 修正依頼中の件数 |

店舗側は当月までの簡易集計、本社側は全期間+期間別の詳細集計、と役割を分担しています。

---

### 11-4. 経費レシート 実OCR読取（v3.17 新規）

> 📌 **撮影したレシートを、APIキー不要のブラウザ内OCRで実際に文字認識**
> 経費レシート登録画面で、スマホで撮影した実画像から日付・金額・購入先・支払方法を自動抽出できます。

#### 経費登録 OCRモード・手入力モード（v3.17.8 新規）

> 📌 **経費登録は、OCR読取モードと手入力モードに対応します。OCRは補助機能であり、手入力モードではOCR結果やサンプル値を使わず、ユーザーが入力した金額・購入先・内容をそのまま登録します。レシート画像は証憑として添付できます。**

経費レシート登録画面の最初に、登録方法を2つから選びます。

| モード | 用途 | 入力方法 |
|---|---|---|
| **① レシートをOCRで読み取る** | レシート画像があり、OCR + AI分類で素早く登録したい | 画像アップロード → 実OCR → 確認画面で修正 → 登録 |
| **② 手入力で登録する** | OCRで読み取れないレシート、手書き、または直接入力したい | フォームに直接入力 → レシート画像は任意添付 → 確認画面で再確認 → 登録 |

**手入力モードの仕様（OCR推定値・サンプル値を一切使わない）:**

- 入力欄: 経費日 / 購入先 / 内容 / 金額（税込）/ 消費税 / 支払方法 / 経費科目 / レシート画像（任意）/ 備考
- 店舗名・担当者はシステム既定値（吉田自動車工業 X-Change本店 / 店長）。確認画面で編集可能
- 本社確定科目（`hqCategory`）は **本社が確認時に設定** するため店舗入力欄では未表示。送信時は経費科目 (`category`) からフォールバックされる
- 「次へ確認」を押すと OCR を一切実行せず、確認画面に直接遷移
- 確認画面では金額・購入先・内容・消費税・支払方法・経費科目・備考をすべて編集可
- 「この内容で登録」押下時、**確認画面の DOM 入力値** から `finalExpenseRecord` を組み立て、localStorage 保存と Google Sheets 送信の両方に **同一オブジェクト** を使用
- 手入力レコードは `ocrText: ""` / `ocrSource: "manual"` / `source: "manual"` / `receiptThumb: "✍️"` で記録。OCR読取結果カードは確認画面で **非表示**

**レシート画像の保存:**

手入力モードで添付した画像は、現在の仕様では `receiptDataUrl` (base64 dataURL) として localStorage に保持し、Google Sheets 02_経費明細 の `receiptImageUrl` カラムには同 dataURL をそのまま送信します。Apps Script 側で Drive へのアップロード・URL 化を行う **外部URL保存は次フェーズ** で実装予定です（現段階では base64 文字列が大きく Sheets セルの上限に達する場合は Apps Script 側で省略保存される可能性があります）。

**デバッグログ（DevTools コンソール）:**

```text
[Expense Manual] final expense record    {...}        // 手入力モードで登録した場合のみ
[Expense] final expense record           {...}        // OCRモードで登録した場合
[Expense] final amount input value       "3000"       // 両モード共通
[Sheets] addExpense sending              {...}        // 両モード共通
```

#### 本番表示のクリーンアップ（v3.17.7）

> 📌 **本番運用画面では、サンプルOCR・デバッグ表示・固定サンプル値は表示しません。売上入力は項目テンプレートのみを表示し、経費登録はOCR読取または手入力登録を選択する方式とします。**

| 削除/非表示 | 対象 | 復活方法（開発時のみ） |
|---|---|---|
| 削除 | 売上音声入力画面の「💡 おすすめの話し方」固定サンプル12項目 | 履歴から復元可 |
| 削除 | 売上音声入力画面の「📋 サンプル音声を入れる」ボタン | 履歴から復元可 |
| 削除 | 経費登録画面の「📋 サンプル経費テキストを入れる」ボタン | 履歴から復元可 |
| 削除 | 経費登録画面下部の OCR デバッグ表示 (OCR source / text length / detected ...) | `console.debug("[v3.17 OCR]", ...)` は引き続き出力 |
| 非表示 | 「サンプルレシートから選ぶ」グリッド | `document.body.classList.add('dev-mode')` で開発時のみ表示 |

#### OCR結果の安全な扱い（v3.17.2 改善）

> ⚠ **OCR読取結果をそのまま登録項目へ流し込みません。**
> 推定に自信がない項目は **空欄または「要確認」** にし、店長による確認・修正を必須化します。OCR全文は「OCR読取結果（原文）」欄にのみ表示し、「内容」欄には短い要約だけが入ります。

| 項目 | 抽出ロジック | 自信がない時の挙動 |
|---|---|---|
| **金額（税込）** | ① `総合計` / `合計（税込）` / `税込合計` ② `お支払金額` / `請求金額` ③ `合計`（小計を除外、最後の出現を採用）④ `税込` ⑤ `クレジット支払` / `カード支払` / `VISA/Master/JCB/AMEX` ⑥ `現金` / `お預かり` / `お買上金額` ⑦ ¥/円マーク付き数字の最大値（≥50円のみ） | 0円のまま空欄。**「金額を確認してください」** で登録ブロック |
| **消費税** | `消費税等` / `税額` / `内消費税` / `消費税(8%)` / `消費税(10%)` キーワード直近の金額。`8`/`10` 単独はレートとして除外 | 高信頼な合計から自動逆算（10%税込前提）。それ以外は空欄 |
| **購入先** | レシート上部の店舗名（セブン-イレブン / 7&i / ローソン / ファミマ / ENEOS / カインズ / コメリ / オートバックス / イエローハット 等を網羅） | 空欄。**「購入先を入力してください」** で登録ブロック |
| **支払方法** | クレジット/VISA/Master/JCB/AMEX → クレジットカード ／ PayPay/d払い/楽天ペイ → QR決済 ／ 現金/お預り/お釣り → 現金 | 空欄。**「支払方法を選択してください」** で登録ブロック |
| **AI経費科目** | 購入先 + OCRテキストからルールベース分類 | 低信頼度時は **未選択** にして店長に手動選択を促す。**「経費科目を選択してください」** で登録ブロック |
| **内容** | 高信頼カテゴリがあれば「ガソリン代」「タイヤ仕入」等の短いラベル ／ 購入先のみなら「<購入先>購入分」 ／ どちらも不明 → **「レシート内容 要確認」** | OCR全文を「内容」欄に流し込まない |

**金額として安易に採用しない数字:** 電話番号 / 会員番号 / 伝票番号 / 承認番号 / 日付 / 時刻 / 商品コード / バーコード / ポイント / 税率（8 / 10）/ 数量 / 50円未満の小さな数字 / OCRノイズ。

**低信頼度バナー:** 金額が ¥/円マーク付きの最大値フォールバックでしか取れなかった場合、または購入先が判定できなかった場合、確認画面に **「⚠ OCRの読み取り精度が低いため、金額・購入先・経費科目を手入力してください。」** バナーを表示し、AI経費科目の自動選択をスキップします。

#### 実OCR読取とサンプルOCR読取の厳密分離（v3.17.1 改善）

> ⚠ 経費レシート登録では、**実OCR読取** と **サンプルOCR読取** を厳密に分けています。
>
> **実OCR読取を行った場合は、アップロードされたレシート画像から読み取った文字列のみを使って、購入先・金額・支払方法・AI経費分類候補を推定します。サンプル経費データは、サンプルボタンを押した場合のみ使用します。**
>
> アップロード画像があるのに OCR が空文字列を返した場合や、OCR読取に失敗した場合でも、**サンプル経費データで勝手に項目を埋めることはありません**（推定できなかった項目は空欄のまま登録確認画面に進みます）。

| シナリオ | 動作 |
|---|---|
| ✅ レシート画像をアップロード → 「OCR読取」 | Tesseract.js で実OCR実行。**サンプルデータは使用しない** |
| ✅ サンプルカードをタップ → 「OCR読取」 | サンプル固定OCRテキストを表示。実OCRは走らせない |
| ✅ 画像なし + メモ入力 → 「OCR読取」 | メモテキストをOCR結果として展開 |
| ✅ 画像なし + メモなし → 「OCR読取」 | 「先にレシート画像をアップロードしてください」を表示。**勝手にサンプルを使わない** |
| ✅ ユーザーがOCR結果を手動修正 → 「AI分類して確認」 | 修正後テキストを最優先で利用 |

画面上部には常に **OCR利用状況チップ**（未読取 / 実OCR結果を使用中 / サンプルOCR結果を使用中 / メモテキストを使用中）が表示され、現在どのソースで動作しているかが一目で確認できます。

開発確認用に画面下部とブラウザの DevTools コンソール (`console.debug("[v3.17 OCR]", ...)`) に **OCRソース・テキスト長・検出した購入先/金額/AI分類** が出力されます（本番表示では小さなグレー文字で目立たないようにしています）。

#### v3.17 で何が変わったか

| 変更点 | 内容 |
|---|---|
| **実OCR読取** | これまで「画像アップロード→モックOCRデータで擬似表示」だった処理を、**Tesseract.js による実際のOCR文字認識** に変更。撮影した実レシートでも動作します。 |
| **ブラウザ内処理 / APIキー不要** | OCR処理は全て利用者のブラウザ内で完結。**画像は外部サーバーに送信されません**。GitHub Pages / Netlify / Vercel など静的ホスティングのままで実OCRが動作します。 |
| **OCRソースの3区分** | 1️⃣ **実OCR読取**（撮影画像 / Tesseract.js）／ 2️⃣ **サンプルOCR読取**（デモ用固定テキスト）／ 3️⃣ **メモテキスト**（画像なし入力）の3つを画面上で明示 |
| **OCR結果の編集** | 読取結果のテキストを **その場で直接編集可能**。誤読をその場で修正でき、編集に応じて 日付/金額/購入先 等のフィールドプレビューが自動更新されます。 |
| **進捗バー表示** | OCRエンジン読込・日本語学習モデル読込・テキスト認識の各フェーズを **0〜100% の進捗バー** で表示 |
| **日付の自動抽出** | OCRテキスト内の `2026/05/03` `2026年5月3日` `2026-05-03` 等の日付表記を自動検出して経費の日付に反映 |
| **撮影モードのヒント** | スマホでは `<input type="file" accept="image/*" capture="environment">` により**背面カメラを直接起動**できます。 |

#### 利用フロー（店舗・スマホ）

1. 経費レシート登録画面を開く
2. 「📷 タップして撮影 / 画像を選択」 → スマホ背面カメラでレシートを撮影
3. 「🔍 OCR読取」をタップ
   - 初回のみ Tesseract.js 本体（約2MB）と日本語学習モデル（約15MB）を CDN からダウンロード（進捗バー表示）
   - 2回目以降はブラウザキャッシュから即起動
4. 認識結果が **編集可能テキストエリア** に表示される
5. 誤読箇所を直接修正 → 下のフィールドプレビュー（日付・金額・購入先・消費税・支払方法）が自動更新
6. 「🤖 AI分類して確認」 → AI経費分類候補（Top3）と共に登録確認画面へ

#### OCR精度に関する注意事項

OCR読取は以下の条件で精度が変わります。読み取り結果は **登録前に必ず確認・修正** してください。

- レシート画像の **明るさ・コントラスト**（暗い場所での撮影は推奨しません）
- **手ブレ・ピンぼけ**（撮影時に少し離れて、フォーカスを合わせてください）
- **文字の向き・歪み**（斜めから撮影すると認識率が下がります）
- **印字の薄さ**（感熱紙レシートで時間が経過したものは特に注意）
- **改行・桁区切り**（金額の `5,000` が `5.000` 等と誤読されることがあります）

そのため、画面上には常に **「読み取り結果は登録前に必ず確認・修正してください」** という注意書きを表示しています。

#### 本番化時のOCR強化（v3.17 → 本番）

> 📌 **本番運用上の注意**
>
> 現在のプロトタイプではTesseract.jsによるブラウザOCRを使用しています。ブラウザOCRはAPIキー不要でデモに適していますが、**日本語レシートの読取精度には限界があります**（手書き・薄い印字・斜め撮影・小さな文字サイズなどで誤認識が発生します）。本番運用では、**Google Vision OCR、Google Document AI、またはレシート専用OCR API をサーバー側で利用し、APIキーを安全に管理する構成**を推奨します。

現在のプロトタイプでは、**APIキーを使わないブラウザOCR（Tesseract.js）** によりレシート画像の文字読取を行います。これによりサーバー不要・追加コスト不要で実OCRデモが可能ですが、本番運用では以下の高精度OCRサービスへの切り替えを推奨します。

| 観点 | プロトタイプ（v3.17） | 本番推奨 |
|---|---|---|
| OCRエンジン | Tesseract.js（ブラウザ内） | **Google Cloud Vision OCR** または **Google Document AI**（Receipt Parser テンプレート） |
| 精度 | 印字レシートで実用レベル、手書き・薄い印字・斜め撮影は弱い | 手書き・低品質画像・縦書き等にも強い、レシート専用パーサあり |
| 構造化抽出 | 自前の正規表現で日付・金額・購入先を抽出 | 日付・店名・小計・税額・支払方法・明細行が **構造化JSONで返却** される |
| 通信 | 画像は外部送信されない | サーバーAPI経由（認証 + データ保護対応） |
| コスト | 無料（CDN通信のみ） | 1,000ページあたり $1.50（Vision OCR）／ Document AI は別料金体系 |
| 認証 | なし | API キー or サービスアカウント JSON、本番では Cloud Functions 経由でのプロキシを推奨 |

本番化時の実装は次の手順で行います:

1. Cloud Functions / Cloud Run に **OCR専用エンドポイント** を作成し、Vision API のキーをサーバー側に閉じ込める
2. フロントエンドからは画像 base64 を POST → サーバーで Vision OCR 呼出 → JSON で日付/購入先/金額/税額/支払方法/明細を返す
3. 既存の `runRealOCR()` 関数を **Cloud Functions 呼出** に差し替え（`parseExpenseText()` の正規表現抽出はフォールバックとして残す）
4. オフライン時 / API障害時のフォールバックとして **Tesseract.js を引き続き利用** することも可能（プログレッシブ・エンハンスメント）

#### 技術構成（v3.17）

```html
<!-- 遅延ロード: 経費レシート登録画面で「OCR読取」を初回タップしたタイミングで CDN から取得 -->
<script src="https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js"></script>
```

```javascript
// 言語: 日本語 + 英語（金額の数字・英字混在に対応）
await Tesseract.recognize(dataUrl, "jpn+eng", {
  logger: (m) => updateOCRProgress(m.status, m.progress)
});
```

- **Tesseract.js v5** を採用（最新の WebAssembly 高速化版）
- 言語コード `"jpn+eng"` で日本語・英語の混在認識（金額・店名のローマ字混在対応）
- 進捗ロガーは Tesseract.js が出力する status 文字列を **日本語ラベルに変換** して表示
- 初回ダウンロード後は IndexedDB / Cache Storage に保存され、2回目以降は瞬時に起動

---

## 12. お客様確認用 公開方法（静的ホスティング）

本プロトタイプは **依存ライブラリ・サーバー処理なし** の純粋な静的Webサイトです。
そのまま静的ホスティングサービスにアップロードして、お客様にURLを共有することで確認いただけます。

### 12-1. 公開前のチェック ✅ 確認済み

| 項目 | 状態 |
|---|---|
| 外部CDN・APIへのリクエスト | **なし**（全て自己完結） |
| ローカルパス (`file:///`) 依存 | **なし** |
| サーバーサイド処理 | **なし**（純粋な静的サイト） |
| CSS/JS の参照パス | 相対パス（`./styles.css` / `./app.js`） |
| `localStorage` での保存 | **対応**（ブラウザだけで完結） |
| CSV出力 | **対応**（Blob + `URL.createObjectURL` でブラウザダウンロード） |
| レシート画像アップロード | **対応**（`FileReader` で base64 化、localStorage に保存） |
| GitHub Pages 互換 | `.nojekyll` ファイル設置済み |

### 12-2. Netlify Drop で公開する（最も簡単・推奨）

1. **プロジェクトフォルダを ZIP 化する**
   - `X-change/` フォルダごと圧縮 → `X-change.zip`
   - macOS: フォルダを右クリック → 「圧縮」
   - Windows: フォルダを右クリック → 「送る」 → 「圧縮 (zip 形式) フォルダー」

2. **Netlify Drop にアクセスする**
   - https://app.netlify.com/drop（Netlify アカウント不要、サインアップなしで利用可）

3. **ZIP ファイル または フォルダ をアップロードする**
   - ブラウザに ZIP ファイル または `X-change/` フォルダごと **ドラッグ＆ドロップ**
   - 数秒で自動デプロイ完了

4. **発行されたURLをお客様へ共有する**
   - 例: `https://random-name-xxxx.netlify.app`
   - URL を変更したい場合は、Netlify の「Site settings」→「Change site name」から自由に設定可能

### 12-3. Vercel で公開する

#### Vercel CLI を使う場合
```bash
npm install -g vercel
cd X-change
vercel              # 初回はログインが必要
# 質問に Enter で進めば自動デプロイ → URL 発行
```

#### Vercel ダッシュボードからアップロードする場合
- https://vercel.com/new にアクセス
- GitHub リポジトリと連携 or プロジェクトフォルダを直接アップロード
- 設定不要（静的サイトを自動検出）

### 12-4. GitHub Pages で公開する

1. GitHub にリポジトリを作成（例: `xchange-prototype`）
2. プロジェクトファイルを push
   ```bash
   cd X-change
   git init
   git add .
   git commit -m "X-Change AI業務管理プロトタイプ"
   git branch -M main
   git remote add origin https://github.com/<ユーザー名>/xchange-prototype.git
   git push -u origin main
   ```
3. GitHub のリポジトリ画面 → **Settings** → **Pages**
4. **Source** を `main` ブランチ・ルートディレクトリに設定
5. 数分後、`https://<ユーザー名>.github.io/xchange-prototype/` で公開
6. プロジェクトには `.nojekyll` ファイルを同梱済みなので、Jekyll処理に関する問題は発生しません

### 12-5. お客様への共有時の注意点

> ⚠️ **公開する前に必ずお客様にお伝えください**

- ✅ **これは確認用プロトタイプです**（本番システムではありません）
- ✅ **本番データは入れないでください**（プロトタイプ環境はバックアップ・冗長性なし）
- ✅ **音声入力、OCR、AI分類はモック実装です**（実APIは未接続。サンプル文/サンプルレシートで動作確認）
- ✅ **ログイン認証は本番化時に追加します**（現状は誰でもアクセス可能）
- ✅ **公開URLを知っている人は閲覧できるため、機密情報は入れないでください**（個人情報・実際の取引先情報・実金額 等は登録しないこと）

### 12-6. 公開後の動作確認チェックリスト

公開URL を開いてから、以下を確認してください。

- [ ] index.html が開き、店舗ダッシュボードが表示される
- [ ] 「店舗 / 本社」 切替ボタンが動作する
- [ ] フッター 「デモデータ初期化」 で初期状態になる
- [ ] 店舗側: 音声売上入力 → サンプル投入 → AIで分解 → 確認 → 登録 が動作
- [ ] 店舗側: 経費レシート登録 → サンプル選択 → OCR読取 → AI分類 → 登録 が動作
- [ ] 本社側: 売上一覧・経費一覧の表示と並び替え・絞り込み
- [ ] 本社側: 確認済みボタン → ステータスが変化
- [ ] 本社側: CSV出力 → ファイルがダウンロードされる
- [ ] 本社側: 月次集計画面の表示
- [ ] スマートフォン（実機 or 開発者ツール）で店舗側UIが見やすい

---

## 13. ファイル構成

```
X-change/
├── index.html       # 全画面マークアップ (SPA)
├── styles.css       # スマホ／PC レスポンシブスタイル
├── app.js           # 状態管理・モックAI・実OCR(Tesseract.js)・CSV出力 等
├── README.md        # 本ファイル（補助金申請資料 兼 仕様書）
└── .nojekyll        # GitHub Pages 用（Jekyll 処理を無効化）
```

---

## 14. 技術詳細・改修履歴

### 13-1. データ保存

すべて `localStorage` のキー `xchange.records.v1` に **配列** で格納。レコードは `type: "sale" | "expense"` で識別。

**確認ステータス**（4種）:
- 未確認 / 確認済み / 修正依頼 / 月次処理済み
- 旧名 (承認待ち / 承認済み) は `loadAll()` で起動時に自動マイグレーション

### 13-2. モック範囲

| 機能 | 動作 |
|---|---|
| 音声録音 | マイクボタン押下→1.2 秒後にサンプル文を自動投入 |
| AI項目分解 | 正規表現ベースの日本語パーサ ([app.js parseVoiceText()](app.js)) |
| OCR | 7種のサンプルレシート（ENEOS/カインズ/タイヤ仕入/廃タイヤ/電気/通信/広告）から固定OCRテキスト |
| AI経費分類 | キーワードベースのスコアリング ([app.js classifyExpense()](app.js)) |
| 即時共有 | 同一 localStorage を 2 ロールで共有 → ロール切替で同期表示 |

### 13-3. CSV出力フォーマット

| ファイル名 | 列数 | 内容 |
|---|---:|---|
| `xchange_sales_<YYYY-MM>.csv` | 15 | 日付/店舗/担当/売上区分/商品名/サイズ/数量/単価/合計/支払/車種/車両番号/作業内容/備考/ステータス |
| `xchange_expenses_<YYYY-MM>.csv` | 12 | 日付/店舗/担当/購入先/内容/金額/消費税/支払/AI分類/本社確定/備考/ステータス |
| `xchange_monthly_summary_<YYYY-MM>.csv` | 6ブロック | サマリー(10項目)+粗利計算+売上内訳4種+経費内訳3種+売上全明細+経費全明細 |

すべて UTF-8 BOM 付きで Excel 互換。

### 13-4. 月次締めステータス（5状態）

| ステータス | 条件 | 月次締め可否 |
|---|---|---|
| データなし | レコード0件 | × |
| 確認待ち | 未確認/修正依頼が残っている | × ブロック |
| 月次締め可能 | 全て確認済み | ○ |
| 月次締め進行中 | 確認済み + 月次処理済み混在 | ○ |
| 月次締め完了 | 全て月次処理済み | × （解除のみ可） |

### 13-5. 主要バージョン履歴

| Ver | 主な変更 |
|---|---|
| v1 | 初版プロトタイプ（11機能） |
| v2 | 店舗5KPI+5大ボタン、本社PC左サイドバー、8KPIダッシュボード |
| v2.1 | `[hidden]` CSS cascade 問題修正、モーダル開閉ロジック堅牢化 |
| v3 | 音声売上入力 実務化（売上区分9種マルチ選択、車種40種マスタ） |
| v3.1 | 経費レシート登録 実務化（12カテゴリ、AI分類/本社確定の分離） |
| v3.2 | 本社確認・承認フロー強化（4ステータス、月次処理済み追加） |
| v3.3 | HQ一覧をカード型に再設計（v3.4で再度テーブルに戻す） |
| v3.4 | HQ一覧をテーブル形式に戻し、列幅%＋省略表示で右切れ防止 |
| v3.5 | 店舗側 修正・再提出フロー（売上/経費別 修正モーダル） |
| v3.6 | 月次集計5セクション化、月次締め処理、CSV出力3種強化 |
| v3.7 | 補助金申請資料用 説明バナー (.biz-note) を主要画面に配置 |
| v3.8 | お客様確認用 静的ホスティング対応（外部依存なし、Netlify Drop / Vercel / GitHub Pages 互換）|
| v3.9 | スマートフォン表示 包括対応（店舗側=スマホ最適化 / 本社側=PCテーブル＋スマホカード自動切替）|
| v3.10 | 売上音声入力をスマホ標準キーボードのマイクを利用する設計に変更（外部音声認識API不要・API料金ゼロ） |
| v3.11 | 大型「録音開始」ボタンを削除し「📱 スマホ音声入力を使う」ボタンに置換。textareaに `lang="ja-JP"` `autocomplete="off"` 等を追加し、スマホ標準キーボード音声入力をメイン導線として明確化 |
| v3.12 | 売上登録確認画面の合計金額入力欄が iPhone 等で右にはみ出す問題を修正（`min-width: 0` 等のflex/grid shrink 制御） |
| v3.13 | 売上音声入力→AI分解→確認画面 の統一仕様。誤認識しやすい項目（金額・数量・支払方法・タイヤサイズ・車両番号）に ⚠️要確認タグ・注意書きを配置。ボタン文言を「✓ 確認して登録」「🎤 音声入力へ戻る」に変更。パーサ強化（タイヤサイズ空白区切り・車両番号ハイフン省略・タイヤ交換キーワード対応） |
| v3.14 | ガイドを「番号付き話す順番」から「項目名形式」に変更。「📝 入力テンプレートを入れる」ボタン追加。AIパーサに構造化フィールド解析（売上区分:/商品名:/タイヤサイズ:/数量:/車種:/車両番号:/合計金額:/支払方法:/作業内容:/備考: などのラベル付き音声入力を自動分解）を追加。自由文章も従来通り分解可。 |
| v3.15 | 音声入力テンプレートから日付・店舗名・担当者を除外し、お客様名・単価を追加した12項目構成に変更。日付/店舗名/担当者はシステム側デフォルト（本日 / 吉田自動車工業 X-Change本店 / 店長）。画面を開いた時点で自動でテンプレート初期表示、クリアボタンも空欄ではなくテンプレート再表示。AIパーサに 顧客名: / 単価: ラベルを追加 |
| v3.16 | 本社サイドバーに「📈 売上集計」メニュー追加。期間別5カード（本日/昨日/当月/前月/全期間）+ 件数4カード + 5種フィルタ（期間/支払方法/区分/ステータス）+ 日別/月別推移 + 内訳4種（支払方法/区分/商品/サイズ）+ 期間指定CSV出力。店舗ダッシュボードに「当月売上」カード追加（5枚→6枚）|
| v3.17 | 経費レシート登録画面に **実OCR読取** を追加。Tesseract.js（ブラウザ内・APIキー不要）で撮影実画像を文字認識。OCRソースを「実OCR読取／サンプルOCR読取／メモテキスト」の3種で明示し、結果テキストはその場で編集可。進捗バー（OCRエンジン読込・日本語モデル読込・テキスト認識のフェーズ別）を表示。`parseExpenseText()` に日付検出（`YYYY/MM/DD` `YYYY年MM月DD日` `YYYY-MM-DD`）を追加。スマホで `capture="environment"` により背面カメラ直接起動。画像は外部送信されず GitHub Pages 等の静的ホスティングのまま動作。本番化時は Google Cloud Vision OCR / Document AI への差替手順を §11-4 に明記。 |
| v3.17.1 | 実OCRとサンプルOCRの **厳密分離** を強化。実画像アップ時は絶対にサンプル経費データへフォールバックしない（OCRが空文字を返しても項目は空欄維持）。`_buildDraftFromUpload()` を実OCR/サンプル/メモ/空 の4系統に明示分岐し `draft.ocrSource` を必須化。画面上部に常時表示の **OCR利用状況チップ**（未読取/実OCR/サンプル/メモ/読取中）と画面下部の **デバッグ表示**（ソース・テキスト長・検出した購入先/金額/分類）を追加。`parseExpenseText()` の購入先候補にコンビニ（セブン-イレブン/ローソン/ファミマ/ミニストップ/イオン他）、支払方法に Master/JCB/AMEX/QR決済（PayPay/d払い/au PAY/楽天ペイ等）/釣銭/お預り を追加。金額検出を「総合計/合計/税込/総額/請求金額」優先 → 漢数字 → 全候補から最大値の3段階に強化。 |
| v3.17.2 | OCR結果の安全な扱いを徹底。OCR全文を「内容」欄や「金額」欄にそのまま流し込まないようにし、専用の `extractAmountFromReceipt()` `extractTaxFromReceipt()` `extractVendorFromReceipt()` `extractPaymentFromReceipt()` `summarizeReceiptContent()` を新規実装。金額抽出を **キーワード優先 7段階**（総合計/合計（税込）/税込合計 → お支払金額/請求金額 → 合計（小計を除外・最後の出現） → 税込 → クレジット支払 → 現金/お預り → ¥/円マーク付き数字の最大値）に再構成し、税率（8/10）・電話番号・伝票番号・50円未満のノイズ数字を採用しない。消費税は `消費税等` / `税額` / `内消費税` / `消費税(8%)` / `消費税(10%)` キーワード直近のみ採用。内容欄には OCR 全文ではなく **短い要約**（「ガソリン代」「<購入先>購入分」「レシート内容 要確認」等）のみを入れる。低信頼度時は AI経費科目の自動選択をスキップし、確認画面に **「⚠ OCRの読み取り精度が低いため、金額・購入先・経費科目を手入力してください。」** バナーを表示。経費登録/上り/確認画面に常時 **「ブラウザOCRはレシートの角度・明るさ・文字の小ささにより誤認識する場合があります」** の注意書きを追加。Safari 16.3 以前の lookbehind 非対応環境でも壊れないよう正規表現を分割。購入先候補に 7&i / オートバックス / イエローハット / イオン / ヨーカドー / ライフ を追加。 |
| v3.17.3 | 開発領域を **3領域に分割管理**（領域A: 売上音声入力・店舗側操作 ／ 領域B: 経費OCR・AI分類 ／ 領域C: 本社確認・月次集計・権限管理）。README §4-3 に領域分割の趣旨・対象機能・主なファイル位置・領域間依存関係・修正時チェックリストを明記。`app.js` 冒頭ヘッダコメントと主要セクション境界（AI音声パーサ層 / 経費パーサ・OCR抽出器 / 共通基盤 / 領域A店舗UI / 領域B経費UI / 領域C本社UI）に `▼▼▼ 領域X: ... ▼▼▼` / `▲▲▲ 領域X: ... (END) ▲▲▲` ディバイダコメントを追加。コードロジックは無変更。経費OCR修正時に売上音声入力を、本社機能修正時に店舗機能を、それぞれ壊さないための保守ガードを文書化。 |
| v3.17.4 | **Google スプレッドシート連携** を追加（Apps Script Web App 経由・APIキー不要・Google Cloud 設定不要）。`XCHANGE_SHEETS_CONFIG` 定数（enabled / endpoint / token）と `sendSaleToSheets()` 共通ヘルパを実装。売上「確認して登録」押下時、localStorage 保存後に fire-and-forget で `action: "addSale"` を Apps Script へ POST。payload は 18項目（saleDate / storeName / staffName / customerName / sellerName / saleCategory / productName / tireSize / quantity / unitPrice / totalAmount / paymentMethod / carModel / carNumber / workContent / memo / overseasSale / confirmStatus）。各売上レコードに `sheetsSyncStatus` (`pending`/`synced`/`failed`) を追加。送信成功/失敗時にそれぞれ「Googleスプレッドシートへ登録しました」「Googleスプレッドシートへの送信に失敗しました。ローカルには登録されています。」をトースト表示。CORS preflight 回避のため `Content-Type: text/plain` で送信。送信失敗時も localStorage 保存・店舗側/本社側 売上一覧・月次集計・CSV出力は通常通り動作（既存機能を壊さない）。詳細は §10-1。 |
| v3.18.0 | **第1分割: 店舗側スマホ画面整理 + 仕入登録 + 本日/今月の状況 + マスタ管理**。店舗ホームのメニューを 7枚に再編 (売上を登録 / 経費を登録 / **仕入を登録** / **本日の状況** / **今月の状況** / 本日の売上一覧 / 本日の経費一覧 / 本社からの修正依頼 / **マスタ管理**)。新規画面: `store-today-status` (6カード: 売上/経費/仕入/概算粗利益/未確認/修正依頼)、`store-month-status` (月初〜本日の累計 6カード + 件数表示)、`store-purchase-upload` / `store-purchase-confirm` / `store-purchase-done` (仕入登録フロー — 仕入日/仕入先/商品名/タイヤサイズ/数量/単価/仕入合計/支払方法/備考)、`store-master` (顧客/商品/経費科目/支払方法の4タブ)。仕入レコード (type:"purchase") を records 配列に追加し、`sendPurchaseToSheets()` で **03_仕入明細** へ送信（`sheetsSyncStatus` pending/synced/failed トラッキング）。顧客マスタ (`xchange.customers.v1`) と商品マスタ (`xchange.products.v1`) を localStorage で管理し、CRUD UI 完備。売上「お客様名」欄に `<datalist id="customerSuggestList">` を実装し、顧客選択時に車種/車両番号/タイヤサイズを自動補完（空欄時のみ）。売上登録時に `_bumpCustomerOnSale()` で最終来店日・累計売上を自動更新。未登録顧客の自動マスタ追加は行わず、トーストで案内のみ（大量登録防止）。仕入の商品名欄にも `<datalist>` 候補、選択時にタイヤサイズ/標準仕入単価/数量×単価=合計 を自動補完。`PAYMENT_METHODS_MASTER = ["現金","カード","QR決済","振込","売掛","引落","その他"]` を統合定義。`EXPENSE_CATEGORIES` に **水道光熱費** を追加 (12→13項目)。既存の売上音声入力 / AI分解 / 売上登録前確認 / 経費OCR登録 / 経費手入力登録 / Google Sheets送信 / localStorage保存 / 本日経費サマリー / 月次集計 / 本社側経費一覧 / 修正依頼 / CSV出力 / スマホ表示は **全て無変更**。本社側の決算用集計・操作ログ・マスタ完全同期は第2/第3分割で対応予定。 |
| v3.17.8 | 経費登録を **OCRモード / 手入力モード** の 2系統に分離。経費レシート登録画面の最初に「① レシートをOCRで読み取る」「② 手入力で登録する」のモード選択カードを表示し、選択後にそれぞれのフォームへ遷移。**手入力モード**: 経費日 / 購入先 / 内容 / 金額（税込）/ 消費税 / 支払方法 / 経費科目 / レシート画像（任意添付）/ 備考 の入力フォーム。OCR推定値・サンプル経費・サンプルOCRテキストを **一切使用しない**（コードで明示的に `_expenseMode === "manual"` を分岐し、`ocrText = ""` / `ocrSource = "manual"` / `source = "manual"` を強制）。手入力モードでも登録前確認画面を必ず通り、確認画面の DOM 入力値で `finalExpenseRecord` を組み立て localStorage 保存と `sendExpenseToSheets()` の両方に使用（v3.17.6 の手入力最優先ロジックを継承）。確認画面では OCR読取結果カードが ocrText 空時に自動非表示。手入力レコードは `[Expense Manual] final expense record` / `[Sheets] addExpense sending` (`source: "manual"`) でデバッグログ出力。**OCRモード**: 既存挙動を維持（実OCR / サンプルOCR / メモ / 空 の 4系統＋ユーザー手修正最優先）。背面カメラ起動・進捗バー・OCR原文編集・AI分類候補・低信頼度バナーは全て継続動作。レシート画像は両モードとも `receiptDataUrl` (base64) として保存・送信（外部URL化は次フェーズ）。既存機能（売上登録 / 売上 Sheets 送信 / 経費 localStorage 保存 / 経費 Sheets 送信 / 本日経費サマリー / 月次集計 / 本社側経費一覧 / 修正依頼 / スマホ表示）は全て無変更。 |
| v3.17.7 | **本番画面のクリーンアップ**。本番運用では、サンプルOCR・デバッグ表示・固定サンプル値は表示しません。売上入力は項目テンプレートのみを表示し、経費登録はOCR読取または手入力登録を選択する方式とします。**売上音声入力画面**: 固定サンプル例 12項目（「お客様名:高橋様」「タイヤサイズ:195/65R15」等）と「💡 おすすめの話し方」見出しを削除し、説明文「項目名に続けて、短く区切って話してください。話し終わったら内容を確認し、必要に応じて手で修正してください。」のみ残す。「📋 サンプル音声を入れる」ボタンを削除（テンプレート投入ボタンは保持）。**経費レシート登録画面**: ① レシートをOCRで読み取る / ② 手入力で登録する の 2 ルートを見出しで明示。biz-note を「OCRは補助機能です。金額や購入先が間違う場合があります。手入力で登録する場合は、入力した内容がそのまま登録されます。」に差し替え。「📋 サンプル経費テキストを入れる」ボタンと、画面下部の OCR デバッグ表示（OCR source / text length / detected vendor / detected total amount / detected category）を削除。「サンプルレシートから選ぶ」グリッドは `.dev-only` クラスで本番非表示化（`body.dev-mode` で開発時のみ表示可能）。アクションバー上の「画像をアップロードした場合は実OCR読取、サンプルレシートを選んだ場合は…」案内も削除。既存機能（売上音声入力 / AI分解 / 売上登録前確認 / 経費OCR登録 / 経費手入力登録 / Google Sheets 送信 / localStorage 保存 / 本社側・店舗側一覧 / 月次集計 / スマホ表示）は全て無変更。 |
| v3.17.6 | 経費登録の金額処理を **最終確認画面の入力値最優先** に修正。「この内容で登録」押下時、`draftExpense.*` ではなく **DOM から直接** 金額・消費税・購入先・内容・支払方法・経費科目・備考を再取得し、`finalExpenseRecord` を組み立てる。localStorage 保存と `sendExpenseToSheets()` への送信は同一の `finalExpenseRecord` を使用し、OCR・AI分類・サンプル値・旧 draftExpense 値で上書きされない保証を確立。共通正規化ヘルパ `parseAmountInput()` を追加（全角数字 `０-９` → 半角、`¥` `￥` `,` `，` `円` 空白 を除去）し、`"3,000"` `"￥3000"` `"¥3,000"` `"１５０００"` `"15,000円"` を全て正しい数値に変換。`#confExpenseAmount` / `#confExpenseTax` を `type="number"` → `type="text" inputmode="numeric"` に変更（カンマ・通貨記号を含む入力を許容、blur で自動正規化）。経費登録ボタン押下時に `console.log('[Expense] final amount input value', ...)` / `console.log('[Expense] final expense record', ...)` / `console.log('[Sheets] addExpense sending', ...)` をデバッグ出力。支払方法・経費科目は active クラスを持つボタンから DOM 直接読み取り。Google Sheets 送信失敗時もローカル保存・経費一覧・本日経費サマリー・月次集計・OCR画面・AI分類画面・スマホ表示は通常通り動作（既存機能を壊さない）。 |
| v3.17.5 | Google スプレッドシート連携を **経費・仕入** にも拡張。共通コア `_postToSheets(action, payload)` に整理し、`sendSaleToSheets()` / `sendExpenseToSheets()` / `sendPurchaseToSheets()` の3ヘルパを公開。経費「この内容で登録」押下時に `action: "addExpense"` を **02_経費明細** へ送信（payload 16項目: expenseDate / storeName / staffName / vendor / content / amount / tax / paymentMethod / aiCategory / hqCategory / receiptImageUrl / ocrText / memo / confirmStatus / hasRevisionRequest / source）。経費レコードにも `sheetsSyncStatus` を付与し、成功時「Googleスプレッドシートへ経費を登録しました」/ 失敗時「Googleスプレッドシートへの経費送信に失敗しました。ローカルには登録されています。」をトースト表示。仕入は専用画面が無いためヘルパ実装のみ（`action: "addPurchase"` → **03_仕入明細**、payload 15項目: purchaseDate / vendor / productName / tireSize / quantity / unitPrice / totalAmount / paymentMethod / invoiceNo / paymentDueDate / paymentStatus 等）— 将来 `type:"purchase"` 画面を追加した際にそのまま呼び出し可能。Apps Script Web App URL / token は v3.17.4 と同一の `XCHANGE_SHEETS_CONFIG` を使用（新規追加なし）。送信失敗時も localStorage 保存・店舗側/本社側 経費一覧・月次集計・CSV出力・OCR画面・AI分類画面・スマホ表示は通常通り動作（既存機能を壊さない）。 |

---

## 15. 補足: 会計処理 / 月次決算 / 補助金申請への活用

### 会計ソフト連携
- 売上CSV / 経費CSV をそのまま freee / マネーフォワード / 弥生 へ取り込み可能
- 本社確定科目が会計仕訳の科目と直結

### 月次決算
- 月次集計CSV 1 ファイルで PL（粗利益）と 内訳（科目別・支払方法別・商品別）を確認
- 月次締め処理でその月のデータを「月次処理済み」にロックし、改竄を防止

### 補助金申請資料
- 月次集計画面のスクリーンショット（5セクション）が、売上 / 経費 / 内訳 / 件数 をひと目で示す資料として利用可能
- 各画面の `.biz-note` 説明バナーが「この画面で何ができるか」を補助金申請担当者に明示

### 電子帳簿保存法
- レシート画像を `receiptDataUrl` として保存（本番では Google Drive 等への永続保管）
- 本番化時は タイムスタンプ・改竄検知 と組み合わせて電子帳簿保存法に対応可能

---

**© 吉田自動車工業 X-Change Prototype**
